package com.example.daypilot.main.mainZone.task

enum class TaskSortOption {
    DATE,
    NAME,
    DURATION,
    DIFFICULTY,
    NEXT_DATE
}

enum class TaskStatusFilter {
    ALL,
    PENDING,
    COMPLETED
}